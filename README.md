# Test repo to demonstrate possible bug in Quarto for tables with cross references in docx output

* Run *render.R* to generate Quarto and R Markdown outputs

* Run *render.sh* to generate pandoc outputs
